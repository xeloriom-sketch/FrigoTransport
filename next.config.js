/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/FrigoTransport',
  images: { unoptimized: true },
}

module.exports = nextConfig
